import { UpdateBicyclePipe } from './update-bicycle.pipe';

describe('UpdateBicyclePipe', () => {
  it('create an instance', () => {
    const pipe = new UpdateBicyclePipe();
    expect(pipe).toBeTruthy();
  });
});
